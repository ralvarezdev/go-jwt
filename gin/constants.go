package gin

var (
	// AuthorizationHeaderKey is the key of the authorization header
	AuthorizationHeaderKey = "Authorization"

	// AuthorizationKey is the key of the authorization in the context
	AuthorizationKey = "authorization"
)
